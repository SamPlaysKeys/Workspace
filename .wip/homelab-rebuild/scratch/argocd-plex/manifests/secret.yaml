# Secret for Plex claim token
# In practice, use sealed-secrets or external-secrets, not plain secrets in Git
apiVersion: v1
kind: Secret
metadata:
  name: plex-secrets
  namespace: media
type: Opaque
stringData:
  claim-token: "claim-xxxxxxxxxxxx"  # Replace or use sealed-secrets
