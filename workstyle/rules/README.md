# Rules

AI-specific rules and constraints for this workspace.

## Active Rules

Rules are loaded automatically by Cursor when working in this repository. They're defined in `.cursor/rules/`.

| Rule | Purpose |
|------|---------|
| `co-creation.mdc` | Core behaviors and conventions for AI collaboration |

## How Rules Work

Cursor rules (`.mdc` files) provide persistent instructions to AI agents. They're loaded automatically based on the `alwaysApply` setting or file path matching.

The rules here are summaries — they point to `workstyle/working_style.md` for full definitions.
